id: mappo
kind: training
description: Centralized critic, decentralized actor training defaults.
algorithm: mappo
policy_mode: ctde
actor:
  observation_scope: local
critic:
  observation_scope: global
optimization:
  learning_rate: 0.0003
  gamma: 0.99
  gae_lambda: 0.95

